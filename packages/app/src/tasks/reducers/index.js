import { newTaskReducer, selectedTaskReducer, taskReducer } from './taskReducer';
import taskListReducer from './taskListReducer';

export { taskReducer, newTaskReducer, selectedTaskReducer, taskListReducer };
