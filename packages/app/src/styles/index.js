/* eslint-disable import/prefer-default-export */
import validationStatus from '../utils/validationStatus';

export const buildStyle = (disabled, styles) => Object.assign({}, styles);
// Object.assign({}, styles, validationStatus(errors));
