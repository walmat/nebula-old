{
  "name": "@nebula/api",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "@babel/cli": "^7.2.0",
    "@babel/core": "^7.2.0",
    "cross-env": "^5.2.0",
    "eslint": "5.6.0",
    "eslint-config-google": "^0.11.0",
    "nodemon": "^1.18.7"
  },
  "dependencies": {
    "aws-sdk": "^2.229.1",
    "cors": "^2.8.4",
    "dotenv": "^6.2.0",
    "express": "^4.16.3",
    "jsonwebtoken": "^8.2.2",
    "moment": "^2.22.2",
    "node-fetch": "^2.1.2",
    "recaptcha-verify": "^0.1.1"
  },
  "scripts": {
    "build": "babel src/ -d build/",
    "ci": "exit 0",
    "dev": "cross-env NODE_ENV=development npm run build && nodemon build/app.js",
    "lint": "eslint src",
    "prestart": "npm run build",
    "prod": "cross-env NODE_ENV=production nodemon build/app.js",
    "start": "npm run prod",
    "test": "echo \"Warning: no test specified\" && exit 0"
  }
}
